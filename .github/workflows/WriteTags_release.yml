name: WriteTags release

on:
  workflow_dispatch:

jobs:
  build:
    uses: FrendsPlatform/FrendsTasks/.github/workflows/release.yml@main
    with:
      workdir: Frends.OPCUA.WriteTags/Frends.OPCUA.WriteTags
    secrets:
      feed_api_key: ${{ secrets.TASKS_FEED_API_KEY }}